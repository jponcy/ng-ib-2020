import { StartWordsPipe } from './start-words.pipe';

describe('StartWordsPipe', () => {
  it('create an instance', () => {
    const pipe = new StartWordsPipe();
    expect(pipe).toBeTruthy();
  });
});
